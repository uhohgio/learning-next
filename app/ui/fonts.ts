import { Inter } from 'next/font/google';
import { Lusitana } from 'next/font/google';
 
export const inter = Inter({ subsets: ['latin'] }); // our primary font
export const lusitana = Lusitana({subsets: ['latin'], weight: '400'}); // secondary